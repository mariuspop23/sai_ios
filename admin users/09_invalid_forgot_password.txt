*** Settings ***
Documentation       Test suite with valid login.
Library             ImapLibrary

Suite Setup         Open Browser To Forgot password
Test Template       Submit other email than valid Should Fail
#Suite Teardown     Close Browser

Resource          ../resource_ios.txt
Resource          ../variables_ios.txt

*** Test Cases ***                  EMAIL                          Error message
Invalid email format                invalid                        Invalid email address
Empty email field                   ${EMPTY}                       Invalid email address
Not existing email in DB            notexistingaddress@test.ro     There is no account registered for this email address

*** Keywords ***

Submit other email than valid Should Fail
    [Arguments]             ${invalid_email}         ${error_message}
    Input Username          ${invalid_email}
    Submit Credentials
    wait until element is enabled                   create_account_error
    element should contain                          css=div.alert.alert-danger      ${error_message}